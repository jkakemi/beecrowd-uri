#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n, x, y, cm;

    cin >> n;
    while(n--){
        cin >> x >> y;
        cm = ((x*y)/2);
        cout << cm << " cm2" << endl;
    }

    return 0;
}
