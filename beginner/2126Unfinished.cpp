#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    unsigned int n, seq;

    cin >> n;
    cin >> seq;

    string cn = to_string(n);
    string cseq = to_string(seq);

    for(int i=0; i<cseq.length(); i++){
        char caracteren = cn[i];
        char caractereseq = cseq[i];
        if(caracteren == caractereseq){
            
        }
    }

    return 0;
}
