#include <bits/stdc++.h>

using namespace std;

int main(){

    int a, b, c;

    cin >> a >> b >> c;

    double s = (double)a/(b+c);

    cout << fixed << setprecision(2) << s << endl;

    return 0;
}