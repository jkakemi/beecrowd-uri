#include <bits/stdc++.h>

using namespace std;

int main(){

    int n, m;

    while(cin >> n >> m && (n != 0 || m != 0)){
        vector<int> pontuacao;
        while(n--){
            int p;
            cin >> p;
            pontuacao.push_back(p);
        }
    }

    return 0;
}